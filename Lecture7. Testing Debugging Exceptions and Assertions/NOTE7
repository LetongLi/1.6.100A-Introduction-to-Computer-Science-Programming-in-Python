Class of debugging
Unit testing
Regression testing
Intergration testing

Testing Approach
Intuition: natural partions
Random testing
Black box testing
Glass box testing

Exception handlers
try:
except;
except xxxError:

else:
finally:

Exceptions dealing methods
fail silently
report Error value
stop execution, signal erroe condition: raise exception("descriptive string")
assert not: The assert keyword lets you test if a condition in your code returns True, if not, the program will raise an AssertionError.


